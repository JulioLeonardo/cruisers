import { Box, styled } from "@mui/material";

const CenterContentContainer = styled(Box)({
  width: "100%",
  height: "70vh",
  display: "grid",
  placeItems: "center",
});

export { CenterContentContainer };
